DATABASE_URL=
JWT_SECRET=change_this_to_a_long_random_secret
PORT=8080

# SMTP configuration for OTP verification (e.g. Brevo)
SMTP_HOST=
SMTP_PORT=
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=

# Optional Firebase Cloud Messaging configuration.
FIREBASE_PROJECT_ID=
FIREBASE_CREDENTIALS_JSON=
